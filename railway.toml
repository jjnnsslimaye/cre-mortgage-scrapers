[build]
builder = "NIXPACKS"

[deploy]
startCommand = "cd florida/broward && python main.py --date $(python -c 'from datetime import datetime, timedelta; print((datetime.now() - timedelta(days=1)).strftime(\"%Y-%m-%d\"))') --upload-to-s3"

[cron]
schedule = "0 6 * * *"
